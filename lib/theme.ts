// Mirrors the --color-glow-* tokens in app/globals.css. Three.js materials
// can't read CSS custom properties, so these hex values are duplicated here
// as the single JS source of truth — update both places together.
export const GLOW_FROM = '#7c3aed';
export const GLOW_VIA = '#6d5ff5';
export const GLOW_TO = '#3b82f6';
export const INK = '#08080b';
